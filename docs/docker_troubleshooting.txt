How to mount new volume on Docker
* Edit dlc-docker inside Docker source code
* Add new variable mypath
* To the docker run command inside script, add "-v $mypath:mypath"
* "-v" command can be repeated multiple times for different variables

If permission denied when downloading ResNet:
Download them manually:
  cd /usr/local/lib/python3.6/dist-packages/deeplabcut/pose_estimation_tensorflow/models/pretrained
  sudo chmod +x download.sh
  sudo ./download.sh
  sudo chown hpc_user:hpc_user resnet_v1_*



